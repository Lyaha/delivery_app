# Delivery Management System

Multi-level web application for managing the delivery process of goods.  
Includes frontend (React), backend (Node.js + Express), and database (PostgreSQL).

## Structure
- **frontend/** — React application
- **backend/** — Node.js + Express + PostgreSQL API
- **docs/** — Project documentation (UA + EN)

## Installation
TBD
